# Python dependencies of the figure and tabulation scripts (scripts/*.py).
# Versions are the ones used for the analyses reported in the manuscript.
numpy==2.5.2
scipy==1.18.0
pandas==3.0.5
matplotlib==3.11.1
